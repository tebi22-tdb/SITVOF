export const environment = {
  production: false,
  /** Vacío para usar el proxy de ng serve: /api → localhost:8081. */
  apiUrl: '',
  /** 0 = desactivado en desarrollo; en producción usar 10. */
  inactivityLogoutMinutes: 0,
};
